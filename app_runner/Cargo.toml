[package]
name = "app_runner"
version = "0.1.0"
edition = "2021"

[dependencies]
env_logger = "0.10.0"
winit = "0.28.6"

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3.9", features = ["libloaderapi", "synchapi", "winbase"] }
